# Desktop client foundation

The desktop client can reuse the TypeScript Memory Core and encrypted snapshot contracts. Recommended implementation: a local desktop shell with filesystem backup and the same explicit connector permissions used by the web client.
