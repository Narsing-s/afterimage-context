'use client';
import {useState} from 'react';
import Link from 'next/link';
export default function MemoryLab(){const[text,setText]=useState('');const[proposals,setProposals]=useState<any[]>([]);const[msg,setMsg]=useState('');const[loading,setLoading]=useState(false);
async function extract(){setLoading(true);setMsg('');try{const r=await fetch('/api/ai/extract',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({text})});const d=await r.json();if(!r.ok)throw new Error(d.error);setProposals(d.proposals||[]);setMsg('AI proposals are suggestions only. Nothing was saved automatically.')}catch(e){setMsg(e instanceof Error?e.message:'Unable to create proposals.')}finally{setLoading(false)}}
return <main style={{minHeight:'100vh',background:'#08090b',color:'#f4f4f5',padding:24,fontFamily:'system-ui'}}><div style={{maxWidth:900,margin:'0 auto'}}><Link href="/" style={{color:'#aaa'}}>← Back</Link><h1 style={{fontSize:52,margin:'42px 0 8px'}}>Memory Lab</h1><p style={{color:'#999',maxWidth:680}}>AI-assisted extraction, retrieval experiments and agent controls. Every AI memory starts as a proposal and requires your approval.</p><section style={card}><h2>AI memory extraction</h2><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Paste a note or conversation..." style={field}/><button onClick={()=>void extract()} disabled={loading||!text.trim()} style={button}>{loading?'Analyzing…':'Create proposals'}</button>{msg&&<p role="status" style={{color:'#aaa'}}>{msg}</p>}{proposals.map(p=><article key={p.id} style={{...card,marginTop:12}}><b>Proposal</b><p>{p.text}</p>{p.trigger&&<p style={{color:'#aaa'}}>Return condition: {p.trigger}</p>}<small>Confidence {Math.round(p.confidence*100)}% · {p.sensitive?'Sensitive input was scrubbed before extraction.':'No sensitive pattern detected.'}</small><p style={{color:'#777'}}>Approval is intentionally manual; connect this proposal to your Memory Vault to save it.</p></article>)}</section><section style={card}><h2>Product capabilities</h2><ul style={{lineHeight:1.9,color:'#bbb'}}><li>Memory Firewall enforcement and agent namespaces</li><li>Version history and supersession metadata</li><li>Signed event primitives and retention policies</li><li>Encrypted sync package primitives</li><li>GitHub, calendar and document connector interfaces</li><li>Retrieval benchmark and anti-annoyance ranking</li><li>MCP-compatible memory tool endpoint</li></ul></section></div></main>}
const card:any={border:'1px solid #292929',background:'#111214',borderRadius:16,padding:20,marginTop:20};const field:any={width:'100%',minHeight:180,boxSizing:'border-box',background:'#090a0c',color:'#fff',border:'1px solid #333',borderRadius:12,padding:14,font:'inherit'};const button:any={marginTop:12,padding:'10px 14px',borderRadius:10,border:'1px solid #444',background:'#17181b',color:'#fff',cursor:'pointer'};
