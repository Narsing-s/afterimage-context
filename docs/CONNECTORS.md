# Context Connectors

Connectors are explicit adapters for documents, GitHub and calendars. Each connector returns minimum-context records with provenance. Real OAuth/API credentials are intentionally not bundled into the core; applications provide authorized fetchers.
