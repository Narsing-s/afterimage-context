# AI and Agents

Afterimage separates AI suggestions from durable memory. Providers implement the AIProvider contract; the default provider is deterministic and local. AI extraction creates proposals that require user approval.

Agents use Memory Firewall permissions and a namespace. Agent actions are audited by the authorization result. MCP-compatible tools expose search, propose, write, update and delete operations through the same boundary.
