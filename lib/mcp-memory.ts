import type {MemoryRecord} from './memory-core';import {assertAgentAction} from './agent-memory-boundary';import type {MemoryAgent} from './memory-firewall';
export async function handleMemoryTool(agent:MemoryAgent,tool:string,args:any,memories:MemoryRecord[]){if(tool==='memory.search'){assertAgentAction(agent,'read',undefined,args.namespace);return memories.filter(m=>!args.query||m.text.toLowerCase().includes(String(args.query).toLowerCase()))}if(tool==='memory.propose'){assertAgentAction(agent,'propose',undefined,args.namespace);return{proposal:args.proposal}}if(tool==='memory.write'){assertAgentAction(agent,'write',args.memory?.id,args.namespace);return{memory:args.memory}}if(tool==='memory.update'){assertAgentAction(agent,'update',args.memoryId,args.namespace);return{memoryId:args.memoryId,patch:args.patch}}if(tool==='memory.delete'){assertAgentAction(agent,'delete',args.memoryId,args.namespace);return{deleted:args.memoryId}}throw new Error('Unknown memory tool.')}
