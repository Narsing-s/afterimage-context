import type {MemoryEvent} from './memory-core';
export async function signMemoryEvent(event:MemoryEvent,secret:string){if(!secret)throw new Error('Signing secret is required.');const key=await crypto.subtle.importKey('raw',new TextEncoder().encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);const bytes=await crypto.subtle.sign('HMAC',key,new TextEncoder().encode(JSON.stringify(event)));return Array.from(new Uint8Array(bytes)).map(b=>b.toString(16).padStart(2,'0')).join('')}
export async function verifyMemoryEvent(event:MemoryEvent,signature:string,secret:string){const expected=await signMemoryEvent(event,secret);if(expected.length!==signature.length)return false;let diff=0;for(let i=0;i<expected.length;i++)diff|=expected.charCodeAt(i)^signature.charCodeAt(i);return diff===0}
