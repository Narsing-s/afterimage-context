import type { VectorIndex } from './local-embeddings';
const DB='afterimage-memory',VERSION=3,STORE='semantic-index',ID='current';
function open(){return new Promise<IDBDatabase>((resolve,reject)=>{if(typeof indexedDB==='undefined')return reject(new Error('IndexedDB unavailable'));const r=indexedDB.open(DB,VERSION);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains(STORE))r.result.createObjectStore(STORE,{keyPath:'id'})};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error||new Error('Unable to open index'))})}
export async function saveVectorIndex(index:VectorIndex){const db=await open();await new Promise<void>((res,rej)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).put({id:ID,index});tx.oncomplete=()=>res();tx.onerror=()=>rej(tx.error||new Error('Unable to save index'))});db.close()}
export async function loadVectorIndex(){const db=await open();const x=await new Promise<VectorIndex|null>((res,rej)=>{const r=db.transaction(STORE,'readonly').objectStore(STORE).get(ID);r.onsuccess=()=>res(r.result?.index||null);r.onerror=()=>rej(r.error||new Error('Unable to load index'))});db.close();return x}
export async function clearVectorIndex(){const db=await open();await new Promise<void>((res,rej)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).delete(ID);tx.oncomplete=()=>res();tx.onerror=()=>rej(tx.error||new Error('Unable to clear index'))});db.close()}
