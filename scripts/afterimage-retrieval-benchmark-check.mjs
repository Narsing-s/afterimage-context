import fs from 'node:fs';const data=JSON.parse(fs.readFileSync('data/retrieval-benchmark.json','utf8'));if(!Array.isArray(data)||data.length<3)throw new Error('Benchmark dataset is too small.');for(const x of data)if(!x.query||!Array.isArray(x.relevantIds)||!x.relevantIds.length)throw new Error('Invalid benchmark case.');console.log('Retrieval benchmark dataset: PASS ('+data.length+' cases)');